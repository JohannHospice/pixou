{
  "name": "firebase",
  "version": "0.0.1",
  "author": "Johann Hospice",
  "license": "MIT",
  "private": true,
  "scripts": {
    "emulate": "firebase emulators:start",
    "deploy": "firebase deploy",
    "deploy:ci": "firebase deploy -P $FIREBASE_PROJECT_ID --token $FIREBASE_TOKEN"
  },
  "devDependencies": {
    "firebase-tools": "^10.2.2"
  }
}
